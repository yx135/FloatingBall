/* 按钮基本样式 */
QPushButton {
    background-color: rgba(255, 255, 255, 180);
    border: none;
    border-radius: 10px;
    padding: 5px;
    color: black;
}

QPushButton:hover {
    background-color: rgba(255, 255, 255, 220);
}

/* 可以添加更多样式... */